{
  "name": "@dexagent/file-intel",
  "version": "0.0.0-scaffold",
  "private": true,
  "description": "Local-first file intelligence: indexing, semantic search, OCR, cross-device retrieval. SCAFFOLD ONLY — implementation gated on Phase G.1 verification spot-checks. See README.md.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'scaffold — no build target until G.3+'",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^1"
  }
}
