services:
  - type: web
    name: openclaw
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: DEX_GATEWAY_PORT
        value: "8080"
      - key: DEX_STATE_DIR
        value: /data/.openclaw
      - key: DEX_WORKSPACE_DIR
        value: /data/workspace
      - key: DEX_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: openclaw-data
      mountPath: /data
      sizeGB: 1
